server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhouhing/SpringCloudDemo.git #配置文件所在仓库
          default-label: master #配置文件分支
          search-paths: Configuration-Center  #配置文件所在根目录